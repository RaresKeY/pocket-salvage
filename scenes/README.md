# Scene Composition

Godot scenes own composition and node relationships. `main.tscn` currently contains only an empty root node reserved for the later game. The temporary startup scene is the [pixel-scaling lab](../labs/pixel_scaling/README.md).

Keep runtime logic in `scripts/`, media in `assets/`, structured content in `data/`, and current subsystem contracts in `specs/` as behavior is added.

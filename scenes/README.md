# Scene Composition

Godot scenes own composition and node relationships. `main.tscn` currently contains only an empty root node and is the startup scene selected by `project.godot`.

Keep runtime logic in `scripts/`, media in `assets/`, structured content in `data/`, and current subsystem contracts in `specs/` as behavior is added.

# Local Instruction Examples

[random-game.md](random-game.md) is the portable repository example. Extract the relevant rules into your own local `AGENTS.md` or `CLAUDE.md`, alongside your environment's engine, container, hardware, and resource constraints.

Both local filenames are always ignored. This directory is tracked so contributors can share repository guidance without imposing one machine's configuration on everyone.

# Controller, mobile Web and checkout rename

RaresKeY, 2026-09-25, verbatim:

> also rename and fix folder name in workspace
> also add explicit controller support and also on screen movile support if detected mobile on web build so p[layer can use on screen on the bottom right controller
